# Legacy reference files
/LEGACY/

# Parked design ideas and speculative notes, kept local until they are ready
/IDEAS/

# Cloned Rust MCP SDK reference
/rust-sdk/

# Local verification work: the audit/findings documents and the harnesses that produced
# them (contract probe, efficiency comparison, schema token measurement).
#
# `benchmarks/` is reserved for PUBLISHED model statements only. Audit records are working
# notes about this repository's own state, so they stay local alongside the tooling.
/benches/

# Cargo build artifacts
/target/


# Python bytecode from the benchmark harnesses
__pycache__/
