# Legacy reference files
/LEGACY/

# Parked design ideas and speculative notes, kept local until they are ready
/IDEAS/

# Cloned Rust MCP SDK reference
/rust-sdk/

# Local verification harnesses (probe/efficiency/schema-token scripts).
# `benchmarks/` is reserved for published model statements; the tooling that produces
# their numbers stays local so the published record is prose, not scripts.
/benches/

# Cargo build artifacts
/target/


# Python bytecode from the benchmark harnesses
__pycache__/
